<% if line_item == @current_item %>
<tr id="current_item">
<% else %>
<tr>
<% end %>
  <td colspan='2'><%= line_item.quantity %>&times;<%= line_item.product.price %></td>
  <td><%= line_item.product.title %></td>
  <td class='line_item_price'><%= number_to_currency(line_item.total_price) %></td>
</tr>
